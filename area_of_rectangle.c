#include <stdio.h>

int main() {
    int length, breadth;

    printf("Length is: ");
    scanf("%d", &length);

    printf("Breadth is: ");
    scanf("%d", &breadth);

    printf("The area of rectangle is %d", length * breadth);

    return 0;
}
